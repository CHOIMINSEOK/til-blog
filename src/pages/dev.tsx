import React from "react"
import styled from "styled-components"

const filePath = "/dev"

const DevArchive: React.FC = () => {
  return <Wrapper>{filePath}</Wrapper>
}

const Wrapper = styled.div``

export default DevArchive
